[tool.poetry]
name = "app"
version = "0.0.1"
description = ""
authors = []

[tool.poetry.dependencies]
python = ">=3.13,<3.14"
mylib = { path = "../mylib", develop = true }

[tool.poetry.group.dev.dependencies]
pyright = "1.1.392"
ruff = "0.7.2"

[tool.pyright]
extraPaths = ["../mylib"]
